import React from "react";
import Nav from "../Nav/Nav";
import Hero from "./Hero";

const Header = () => {
  return (
    <div className="hero">
      <Nav />
      <Hero />
    </div>
  );
};

export default Header;
